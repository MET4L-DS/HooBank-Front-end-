import React from "react";

const Button = () => {
    return (
        <button
            className={`bg-blue-gradient mt-10 rounded-lg px-6 py-4 font-poppins text-[18px] font-medium text-primary outline-none`}
        >
            Get Started
        </button>
    );
};

export default Button;
